{% for product in listProducts %}
      <div class="col-sm-2">
        <h3>
         <form action="{{ path('sendProduct',{ 'product_name' : product.name}) }}">
          <button type="input" class="btn-xlarge btn-primary btn-rounded">{{ product.name }}</button>
         </form>
        </h3>
      </div>
  {% endfor %}